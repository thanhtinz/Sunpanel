key: redis
name:
  vi: Redis
  en: Redis
description:
  vi: "Bộ nhớ đệm và hàng đợi trong RAM, thứ hầu hết ứng dụng web cần tới sớm hay muộn."
  en: "An in-memory cache and queue that most web apps need sooner or later."
category: database
website: https://redis.io
portField: PORT
versions:
  - name: "8.10.0"
    images:
      - redis:8.10.0-alpine
    values:
      IMAGE: "redis:8.10.0-alpine"
  - name: "7.4.10"
    images:
      - redis:7.4.10-alpine
    values:
      IMAGE: "redis:7.4.10-alpine"
  - name: "6.2.23"
    images:
      - redis:6.2.23-alpine
    values:
      IMAGE: "redis:6.2.23-alpine"
fields:
  - key: PORT
    label: {vi: Cổng, en: Port}
    help:
      vi: "Chỉ mở ra Internet khi thật sự cần: Redis không có tường lửa riêng."
      en: "Only expose this to the Internet if you truly must: Redis has no firewall of its own."
    type: port
    default: "6379"
    required: true
  - key: PASSWORD
    label: {vi: Mật khẩu, en: Password}
    type: password
    generate: true
compose: |
  services:
    app:
      image: ${IMAGE}
      container_name: ${CONTAINER_NAME}
      restart: unless-stopped
      command: redis-server --requirepass ${PASSWORD} --appendonly yes
      ports:
        - "${PORT}:6379"
      volumes:
        - data:/data

  volumes:
    data:
